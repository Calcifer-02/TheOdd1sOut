# ИМОЛТ — расчёт вывоза и утилизации строительных отходов

| Версия | Дата | Автор |
|---|---:|---|
| 1.0 | 18.09.2026 | Александр Прокошев |

Сервис считает стоимость вывоза и утилизации отходов строительства и сноса,
подбирает полигоны с ценами и статусом приёма и готовит коммерческое предложение.
Пользователи — заказчики работ, перевозчики и демонтажные компании Москвы
и Московской области.

> **Состояние репозитория: плацдарм.** Службы поднимаются, видят друг друга
> и отвечают на запросы работоспособности; предметной логики расчёта, сценариев
> чат-бота и экранов мини-приложения в них ещё нет. Разделы ниже отмечают, что
> уже работает, а что только предстоит.

## Что это за проект

Цель `L0` и предложение ценности — в [уставе проекта](./docs/ПАКЕТ_АНАЛИТИКИ/УСТАВ_ПРОЕКТА.md)
и [видении и границах](./docs/ПАКЕТ_АНАЛИТИКИ/ВИДЕНИЕ_И_ГРАНИЦЫ.md); формулировки
требований — в [реестре требований](./docs/ПАКЕТ_АНАЛИТИКИ/РЕЕСТР_ТРЕБОВАНИЙ.md)
(`R-001` — `R-065`). Условия направления, в котором делается решение, перенесены
в [условия трека](./client/УСЛОВИЯ_ТРЕКА_ЭФФЕКТИВНЫЙ_БИЗНЕС.md).

## Основной пользовательский сценарий

Приоритетный путь `UC-001`: пользователь вводит адрес вывоза, выбирает типы отходов
и объём, получает расчёт с раздельным учётом перевозки и утилизации, сравнивает
полигоны по итоговой стоимости и выгружает коммерческое предложение. Полный разбор
путей `UC-001` — `UC-009` и макеты экранов — в [запросе на дизайн](./ux/ЗАПРОС_НА_ДИЗАЙН.md)
и файлах `ux/*.dc.html`.

**Сценарий пока не реализован в коде.** Сегодня проверяется только связность служб
(раздел «Пошаговый сценарий проверки»).

## Состав и архитектура

| Служба | Назначение | Технологии | Порт внутри | Порт снаружи |
|---|---|---|---:|---|
| `db` | Хранилище справочников, расчётов и заявок | PostgreSQL 17 | 5432 | `POSTGRES_PORT` |
| `api` | Расчётная часть: справочники, расчёт, коммерческое предложение | .NET 10, ASP.NET Core, Npgsql | 8080 | `API_PORT` |
| `bot` | Чат-бот MAX: приём обновлений платформы | .NET 10, ASP.NET Core | 8080 | `BOT_PORT` |
| `miniapp` | Мини-приложение MAX: интерфейс расчёта | React 19, Vite 7, nginx | 80 | `MINIAPP_PORT` |

Связи: `miniapp → api` через проксирование пути `/api` внутри nginx, `api → db`
по строке подключения, `bot → api` по внутреннему адресу. Наружу открыты все
четыре порта, чтобы каждую службу можно было проверить отдельно.

## Как запустить локально

```sh
cp .env.example .env          # один раз: подставьте пароль базы и токен чат-бота
docker compose up --build     # одна команда поднимает все локальные компоненты
```

Нужны только Docker и Docker Compose; .NET SDK и Node.js для запуска не требуются —
сборка идёт внутри образов. Сборка с нуля занимает около 16 секунд при уже
загруженных базовых образах.

## Порядок остановки и повторного запуска

```sh
docker compose stop            # остановить, данные сохранить
docker compose start           # поднять снова
docker compose down            # снести контейнеры и сеть, тома оставить
docker compose down --volumes  # снести вместе с данными базы
```

После `down` повторный запуск — та же команда `docker compose up --build`.

## Переменные окружения

Образец со значениями по умолчанию — [`.env.example`](./.env.example). Рабочих
токенов, паролей и ключей в репозитории нет: файл `.env` в отслеживание не попадает.

| Переменная | Назначение | Значение в образце |
|---|---|---|
| `API_PORT` | Порт расчётной части на машине | `18080` |
| `BOT_PORT` | Порт чат-бота | `18081` |
| `MINIAPP_PORT` | Порт мини-приложения | `15173` |
| `POSTGRES_PORT` | Порт базы данных | `15432` |
| `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD` | Учётные данные базы | `imolt`, `imolt`, заглушка |
| `MAX_BOT_TOKEN` | Токен чат-бота MAX, выдают организаторы | пусто |
| `MAX_API_BASE_URL` | Адрес интерфейса чат-ботов MAX | пусто |
| `MINIAPP_PUBLIC_URL` | Публичный адрес мини-приложения по HTTPS | пусто |

Порты по умолчанию смещены от привычных 8080, 5173 и 5432: на машине разработчика
они обычно заняты, и запуск падает с `port is already allocated`.

## Зависимости

| Компонент | Чем зафиксированы версии |
|---|---|
| `api`, `bot` | `packages.lock.json` рядом с каждым проектом; в образе восстановление идёт в режиме замка и падает при расхождении |
| `miniapp` | `package-lock.json`; в образе выполняется `npm ci` |
| Базовые образы | Теги закреплены в `Dockerfile`: `sdk:10.0`, `aspnet:10.0`, `node:24-alpine`, `nginx:1.29-alpine`, `postgres:17-alpine` |

## Внешние сервисы и интеграции

| Сервис | Для чего нужен | Состояние |
|---|---|---|
| Платформа MAX | Среда решения: чат-бот и мини-приложение | не подключена; мини-приложение требует адреса по HTTPS, вебхук чат-бота — внешнего адреса. Внутри Docker не воспроизводится |
| Подсказки адресов | Ввод адреса вывоза (`R-012`) | не подключены; поставщик не выбран |
| Расчёт расстояний и внешние карты | Маршрут до полигона (`R-005`, `R-010`) | не подключены; поставщик не выбран |

## Работа с данными

База данных поднимается пустой: схема и миграции не заведены — способ их ведения
выбирается вместе с первой предметной областью и записью архитектурного решения.
Справочники групп отходов, полигонов и тарифов, показанные в макетах, пока живут
только в макетах как демонстрационные значения.

**Тестовых данных в репозитории нет.** Когда они появятся, их происхождение
указывается явно: условия трека (разд. 4 п. 11 и подразд. 8.4) требуют отличать
данные официального источника от подготовленных и модельных.

## Пошаговый сценарий проверки

```sh
sh tests/devops/проверка-запуска.sh
```

Скрипт разбирает `compose.yaml`, сверяет состав служб, поднимает решение одной
командой, проверяет ответы и убирает за собой. Ожидаемый вывод:

```text
расчётная часть: живость /health                ок (200)
расчётная часть: готовность /ready (видит базу) ок (200)
чат-бот: живость /health                        ок (200)
мини-приложение: страница                       ок (200)
мини-приложение → расчётная часть через /api    ок (200)
чат-бот принимает вебхук                        ок (200)
Плацдарм развёртывания проверен: отказов нет.
```

То же вручную, с примерами ожидаемого поведения:

| Запрос | Ожидаемый ответ |
|---|---|
| `curl http://localhost:18080/health` | `{"service":"api","status":"ok"}` |
| `curl http://localhost:18080/ready` | `{"service":"api","status":"ready","database":"reachable"}` |
| `curl http://localhost:18081/health` | `{"service":"bot","status":"ok","token_configured":false}` |
| `curl http://localhost:15173/api/health` | ответ расчётной части, полученный через мини-приложение |
| Открыть `http://localhost:15173/` | страница мини-приложения со строкой «Расчётная часть отвечает» |

Если расчётная часть отвечает `not_ready`, в теле ответа названа причина: не задана
строка подключения или база недоступна.

## Известные ограничения

1. Предметной логики нет: расчёт, подбор полигонов, коммерческое предложение,
   сценарии чат-бота и экраны мини-приложения не реализованы.
2. Схемы базы данных и миграций нет.
3. Решение не подключено к платформе MAX, а условия трека требуют MAX как среду
   реализации и делают это условием допуска. Разрыв назван в разделе 9
   [условий трека](./client/УСЛОВИЯ_ТРЕКА_ЭФФЕКТИВНЫЙ_БИЗНЕС.md) и на 18.09.2026
   не закрыт требованиями реестра.
4. Внешние интеграции (подсказки адресов, расстояния, карты) не выбраны.
5. Способ запуска проверен на машине разработчика; проверка «с чистой машины»
   не выполнялась.

## Структура каталогов

```text
src/back/     расчётная часть и чат-бот: решение .NET (Imolt.Api, Imolt.Bot)
src/front/    мини-приложение MAX (miniapp)
tests/        проверки; tests/devops — проверка запуска
devops/       запуск и эксплуатация: ЗАПУСК.md
docs/         аналитика: требования, решения, артефакты продукта и бизнеса
ux/           запрос на дизайн и макеты экранов
client/       материалы заказчика и условия трека
compose.yaml  запуск всех локальных компонентов одной командой
trip.json     политика проверок git-следа; валидаторы — в trip/
```

Код раскладывается модульным монолитом: каталог на предметную область, внутри —
роли мест. Порядок объявления раскладки — [РАСКЛАДКА_КОДА.md](./docs/РАСКЛАДКА_КОДА.md);
на 18.09.2026 раскладка средству не объявлена, областей ещё нет.

## Куда писать дефекты

В [реестр дефектов](./docs/ПАКЕТ_АНАЛИТИКИ/РЕЕСТР_ДЕФЕКТОВ.md): одна запись —
один `BUG-id` со ссылкой на затронутое требование.

## Журнал контрольных точек

Текущая стадия не хранится в этом файле, чтобы не расходиться с источником:
она выводится из [журнала контрольных точек](./docs/ПАКЕТ_АНАЛИТИКИ/ЖУРНАЛ_КОНТРОЛЬНЫХ_ТОЧЕК.md).

## Пакет заморозки

На контрольной точке в пакет входят: зафиксированная версия исходного кода с хешем
коммита, этот `README.md`, [журнал изменений](./CHANGELOG.md), пакет аналитики
`docs/`, макеты `ux/` и конфигурация запуска (`compose.yaml`, `Dockerfile`,
`.dockerignore`, `.env.example`). Содержание артефактов в пакете не дублируется —
пакет ссылается на них.

## Ключевые артефакты

- [Устав проекта](./docs/ПАКЕТ_АНАЛИТИКИ/УСТАВ_ПРОЕКТА.md)
- [Видение и границы](./docs/ПАКЕТ_АНАЛИТИКИ/ВИДЕНИЕ_И_ГРАНИЦЫ.md)
- [Реестр требований](./docs/ПАКЕТ_АНАЛИТИКИ/РЕЕСТР_ТРЕБОВАНИЙ.md)
- [Условия трека «Эффективный бизнес»](./client/УСЛОВИЯ_ТРЕКА_ЭФФЕКТИВНЫЙ_БИЗНЕС.md)
- [Запрос на дизайн](./ux/ЗАПРОС_НА_ДИЗАЙН.md)
- [Порядок запуска](./devops/ЗАПУСК.md)
- [Правила проекта](./AGENTS.md)
- [Журнал изменений](./CHANGELOG.md)
