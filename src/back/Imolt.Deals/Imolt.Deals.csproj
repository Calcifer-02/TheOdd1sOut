<Project Sdk="Microsoft.NET.Sdk">

  <!-- Область «сделка» (ADR-0001). Наружу открыты только зоны Ports и
       Contracts; ссылок на другие области нет. -->
  <ItemGroup>
    <ProjectReference Include="..\Imolt.Shared\Imolt.Shared.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Dapper" Version="2.1.86" />
    <PackageReference Include="Npgsql" Version="10.0.3" />
    <!-- Печать коммерческого предложения. PdfSharp 6 — лицензия MIT и без зависимостей;
         QuestPDF удобнее, но его Community-лицензия привязана к выручке
         компании, а ИМОЛТ — действующий бизнес. -->
    <PackageReference Include="PdfSharp" Version="6.2.4" />
  </ItemGroup>

</Project>
