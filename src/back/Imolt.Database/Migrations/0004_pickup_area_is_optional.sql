-- Зона обслуживания адреса вывоза становится необязательной.
--
-- Договор объявляет `area` необязательным полем PickupAddress: подсказка
-- адреса зону называет, а адрес, пришедший без неё, остаётся пригодным для
-- расчёта — плечо перевозки считается по координатам. Обязательный столбец
-- вынуждал бы подставить зону за клиента, то есть выдумать её.
--
-- @req: R-012

alter table calculation
  alter column pickup_area drop not null;
