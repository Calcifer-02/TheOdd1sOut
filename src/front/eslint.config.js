// Пресет движка линтера кода TRIP. Правила проекта дописываются после него,
// а не вместо: пресет обновляется средством, и разошедшаяся копия перестаёт
// получать исправления (AGENTS.md, разд. «Связка инструментов TRIP»).
export { default } from '@trip/eslint-config';
