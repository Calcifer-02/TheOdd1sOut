# 🗺️ Структура роутинга приложения

## Созданные маршруты

### ✅ Главная страница
**Путь:** `/`  
**Файл:** `src/app/page.tsx`  
**Описание:** Приветственная страница с "Hello World"

### ✅ Панель управления
**Путь:** `/dashboard`  
**Файл:** `src/app/dashboard/page.tsx`  
**Описание:** Дашборд с статистикой и быстрыми действиями

### ✅ Доступность
**Путь:** `/accessibility`  
**Файл:** `src/app/accessibility/page.tsx`  
**Описание:** Настройки доступности (размер текста, контрастность, голосовое управление)

### ✅ Профиль
**Путь:** `/profile`  
**Файл:** `src/app/profile/page.tsx`  
**Описание:** Профиль пользователя

### ✅ О приложении
**Путь:** `/about`  
**Файл:** `src/app/about/page.tsx`  
**Описание:** Информация о приложении и команде

---

## 📁 Структура App Router

```
src/app/
├── layout.tsx                 # Корневой layout с навигацией
├── page.tsx                   # Главная страница (/)
├── globals.css                # Глобальные стили
│
├── api/                       # API routes
│   └── (будущие endpoints)
│
├── layouts/                   # Дополнительные layouts
│
├── dashboard/                 # /dashboard
│   └── page.tsx
│
├── accessibility/             # /accessibility
│   └── page.tsx
│
├── profile/                   # /profile
│   └── page.tsx
│
└── about/                     # /about
    └── page.tsx
```

---

## 🧭 Навигация

### Компонент Navigation
**Файл:** `src/components/layout/Navigation.tsx`

Responsive навигационное меню с:
- ✅ Активным состоянием для текущей страницы
- ✅ Адаптивным дизайном (desktop/mobile)
- ✅ Иконками для каждого пункта меню
- ✅ Client-side navigation через Next.js Link

---

## 🎨 Стилизация

Каждая страница использует:
- **Tailwind CSS** для utility-классов
- **Градиенты** для фона
- **Тени** для карточек
- **Hover эффекты** для интерактивности

---

## 🚀 Как добавить новый роут

### 1. Создайте папку в `src/app/`
```bash
mkdir src/app/new-page
```

### 2. Создайте `page.tsx`
```typescript
export default function NewPage() {
  return (
    <div className="min-h-screen p-8">
      <h1>Новая страница</h1>
    </div>
  );
}
```

### 3. Добавьте в навигацию (опционально)
Отредактируйте `src/components/layout/Navigation.tsx`:
```typescript
const links = [
  // ...existing links
  { href: '/new-page', label: 'Новая', icon: '🆕' },
];
```

---

## 🔗 Динамические роуты (будущее)

### Пример: `/profile/[id]`
```
src/app/
└── profile/
    ├── page.tsx              # /profile
    └── [id]/
        └── page.tsx          # /profile/123
```

### Пример кода:
```typescript
export default function UserProfile({ params }: { params: { id: string } }) {
  return <div>User ID: {params.id}</div>;
}
```

---

## 🎯 Рекомендуемые дополнительные роуты

### Для социального трека:
- `/challenges` - Социальные челленджи
- `/community` - Сообщество
- `/achievements` - Достижения
- `/settings` - Настройки
- `/help` - Помощь

### Для доступности:
- `/accessibility/voice` - Голосовое управление
- `/accessibility/contrast` - Настройки контрастности
- `/accessibility/text` - Размер текста

---

## 📱 Адаптивность

Все страницы адаптивны:
- 📱 **Mobile**: < 768px
- 💻 **Tablet**: 768px - 1024px
- 🖥️ **Desktop**: > 1024px

---

## ✅ Что уже работает

1. ✅ Next.js App Router настроен
2. ✅ Tailwind CSS интегрирован
3. ✅ TypeScript настроен с path aliases
4. ✅ 5 страниц созданы
5. ✅ Навигация работает
6. ✅ Адаптивный дизайн
7. ✅ Конфигурационные файлы готовы

---

## 🚦 Следующие шаги

1. Установить зависимости: `npm install`
2. Запустить dev сервер: `npm run dev`
3. Открыть http://localhost:3000
4. Начать разработку основной фичи

---

**Создано:** 5 ноября 2025  
**Статус:** Готово к разработке ✅

